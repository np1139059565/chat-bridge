[pytest]
testpaths = tests
pythonpath = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q --strict-markers --disable-warnings
markers =
    e2e: end-to-end tests requiring browser
    unit: unit tests without browser
    api: Flask API tests
