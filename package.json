{
  "name": "chat-bridge-testing",
  "version": "1.0.0",
  "private": true,
  "description": "Testing dependencies for chat-bridge (stylelint only; e2e uses Python Playwright)",
  "scripts": {
    "stylelint": "stylelint \"chrome extension/dialog/style.css\"",
    "stylelint:fix": "stylelint \"chrome extension/dialog/style.css\" --fix"
  },
  "devDependencies": {
    "stylelint": "^16.6.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
